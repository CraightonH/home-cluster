# PLACEHOLDER SECRET - NEEDS ENCRYPTION
# To encrypt this secret, run:
# sops --encrypt --in-place secret.sops.yaml
#
# Make sure to set a strong password before encrypting
apiVersion: v1
kind: Secret
metadata:
    name: enshrouded-secret
    namespace: games
stringData:
    password: "CHANGEME-SetYourPasswordHere"
# After setting the password above, encrypt this file with:
# sops --encrypt --in-place secret.sops.yaml
